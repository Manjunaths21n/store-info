generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model StoreInfoStatus {
  id     String  @id @default(auto()) @map("_id") @db.ObjectId
  status Boolean

  @@map("store-info-status")
}